export const metadata = {
  title: "Public Bulk Pass Request | Harbor Entry Pass System",
  description: "Submit a bulk pass request for multiple visitors to Chennai Port",
};

export default function BulkPassRequestLayout({ children }) {
  return children;
}
